bundle:
  name: git_dbx_cicd

include:
  - resources/*.yml

targets:
  dev:
    default: true
    workspace:
      host: ${{ secrets.DATABRICKS_HOST }}

  prod:
    workspace:
      host: ${{ secrets.DATABRICKS_HOST }}