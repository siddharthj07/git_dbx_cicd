bundle:
  name: git_dbx_cicd

include:
  - resources/*.yml

targets:
  dev:
    default: true
    workspace:
      host: ${env.DATABRICKS_HOST}

  prod:
    workspace:
      host: ${env.DATABRICKS_HOST}