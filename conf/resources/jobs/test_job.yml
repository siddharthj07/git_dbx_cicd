resources:
  jobs:
    test_job:
      name: testing_job
      description: Workflow for test
      timeout_seconds: 0
      max_concurrent_runs: 1
      access_control_list:
        - user_name: "francisdev007@gmail.com"
          permission_level: IS_OWNER
        - user_name: "itishreebswl@gmail.com"
          permission_level: IS_OWNER
      job_clusters:
        - job_cluster_key: job_key_1
          new_cluster: ${var.batch_workflow_cluster}
      tasks:
        - task_key: task_1
          notebook_task:
            notebook_path: /Workspace/Repos/dbx-repo/git_dbx_cicd/notebooks/sid_test
          max_retries: 2
          min_retry_interval_millis: 300000
          retry_on_timeout: true
          job_cluster_key: job_key_1
      tags:
        owner: Sid